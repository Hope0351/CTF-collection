service serv
{
    disable     = no
    type        = UNLISTED
    id          = serv
    socket_type = stream
    protocol    = tcp
    port        = 1337
    wait        = no
    user        = user
    group       = user
    server      = /app/jukebooox
    flags       = REUSE
    server_args = ""
}
